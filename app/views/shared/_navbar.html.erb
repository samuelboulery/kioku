<% if !current_page?(new_photographer_session_path) %>
<div class="navbar">
  <div class="logo" >
    <%= image_tag("logo.svg", class:"w-12") %>
  </div>
  <% if current_photographer %>
    <%= button_to "Log out", signout_photographer_path, method: :delete, class: "button-logout" %>
  <% else %>
    <%= button_to "Log out", signout_school_path, method: :delete, class: "button-logout" %>
  <% end %>
</div>
<% end %>
<div style="background-image: url('<%= image_path "banner.svg" %>');" class="banner">
  <h1>School's Dashboard</h1>
  <h2>Manage your classrooms folder below</h2>
</div>

<div class="absolute right-0 mr-48">
<div class="flex mx-auto ">
  <div class="max-w-xs rounded-lg overflow-hidden shadow bg-white z-1 " style="margin-top: -140px">
    <div class="px-10 pt-6 pb-10">
      <div class="text-4xl text-purple h-8">School</div>
      <div class="font-bold text-4xl text-purple pb-8 border-b-2"><%= current_school.name %></div>
      <div class="flex justify-between">
        <div class="flex py-3 font-bold text-lg justify-start pt-3">Your profit:</div>
          <div class="flex justify-start">
            <p class="pt-3 text-lg text-gray-500">£ </p>
            <p class="py-3 font-bold text-lg pl-1">3 309</p>
          </div>
      </div>
        <div class="flex py-3 font-bold text-lg justify-start pt-4">Photographer:</div>
          <div class="flex justify-between">
            <div class="flex justify-start">
              <p class="pt-3 text-lg text-gray-500 pb-8 border-b-2"><%= current_school.photographer.fullname %></p>
              <p class="pt-3 text-lg text-gray-500 pb-8 border-b-2"><%= current_school.photographer.email %></p>
            </div>
          </div>
    </div>

  </div>
  </div>
<% if current_school.validation? %>
<%= link_to director_update_path, method: :post do%> <!-- status to be changed -->
  <button class= "btn btn-pink mt-16 mr-2"> Validates all the classrooms</button>
  <% end %>
<% end %>
</div>

<div class="containbox">
  <% @classrooms.each do |classroom| %>
    <%= link_to director_classroom_path(classroom.id) do %>
      <div class="folder-svg shadow-xl rounded-md h-24">
        <%= render "shared/folder_svg", color: ["purple", "green", "yellow", "red"].sample %>
        <p class="flex font-bold text-black m pl-5" style="margin-top: -35%"><%= classroom.name %></p>
      </div>
    <% end %>
  <% end %>
</div>



